# Token Monitor Skill

This skill estimates the token usage of the current Antigravity/Claude Code session.

When the user runs `/token` or `/token-monitor`, you must:
1. Run the `token-monitor` shell command.
2. Output the exact token monitor stats and limits table directly to the user. Do not truncate or summarize the output.
